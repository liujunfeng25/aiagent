# Python
__pycache__/
*.py[cod]
*.pyd
.Python
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Envs
.env
.env.*
# 允许提交环境变量模板（供新 clone 的机器复制为 .env）
!.env.example
!**/.env.example
# 本地配置副本（可选，与 config.py 同级变量名对齐）
backend/config_副本.py
venv/
.venv/
ENV/

# Node / Frontend
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Project runtime data
# 【临时】已取消对 backend/data 的忽略（可提交 ai_agent.db、缓存目录等）。换机 pull 后若要恢复为不跟库：
#   1. 在本段上方加一行：backend/data/
#   2. 删除本段三条注释
#   3. 执行：git rm -r --cached backend/data && git add .gitignore && git commit -m "chore: stop tracking backend/data"
# 注意：documents 下可能有上传票据，勿推公共仓库。
backend/*.db
backend/preset_vegetable_model/

# OS / Editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log
